/* Exemplo de Programa na linguagem L */

int n;
char nome[40];
final MAXITER = 10;

/* Programa Principal */
main {
  write("Digite seu nome: ");
  readln(nome);
  for (n:=1; n < MAXITER; n := n + 1) {
    writeln("Olá' ",nome);
  }
}
