/* Exemplo de Programa na linguagem L */

int nome, n;
char a[9];
final MAXITER=10;

main {
/* Programa Principal */
  a := 'a';
  n[9] = 0;
  write("Digite seu nome: ");
  readln(nome);
  for (n:=1; n<=MAXITER; n:=n+1)
    writeln("Ola ",nome);
}
