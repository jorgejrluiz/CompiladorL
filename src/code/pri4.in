/* Exemplo de Programa na linguagem L */

int n:=-2;
char nome[40];
final MAXITER=10;

/* Programa Principal */
main {
  write("Digite seu nome: ");
  readln(nome);
  n:=1+(3*4);
  for (n:=1; n < MAXITER; n:= n + 1) {
    writeln("Ola' ",nome);
  }
}